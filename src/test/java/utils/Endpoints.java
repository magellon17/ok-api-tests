package utils;

public class Endpoints {
    public static final String getGroupCounters
            = "/fb.do?application_key={application_key}&counterTypes={counterTypes}&format={format}&group_id={group_id}&method={method}&sig={sig}&access_token={access_token}";
}
